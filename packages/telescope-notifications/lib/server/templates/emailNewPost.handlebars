<span class="heading">
  <!-- Anonymizing
<a href="{{profileUrl}}">{{postAuthorName}}</a>
-->
Someone has posted a new idea:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{postTitle}}</a>
{{else}}
  {{postTitle}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="post-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{postUrl}}">Discuss</a><br><br>
